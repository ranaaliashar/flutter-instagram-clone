import 'package:flutter/material.dart';

class WebScr extends StatelessWidget {
  const WebScr({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Web screen')),
    );
  }
}